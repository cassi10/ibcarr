import Head from "next/head";

const Home = (): JSX.Element => {
  return (
    <Head>
      <title>Games</title>
      <meta name="description" content="Games." />
    </Head>
  );
};

export default Home;
